# Empty on purpose. The deployed site calls its own origin and Vercel forwards
# /api, /Images and /Files to the API (see vercel.json). Keeping the browser on
# one https origin avoids mixed content — the API itself is http only — and
# removes CORS from the picture entirely, since nothing is cross-origin.
VITE_API_URL=
